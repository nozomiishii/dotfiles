- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
# - create:
#     - ~/testing
# - shell:
#     - [git submodule update --init --recursive, Installing submodules]
