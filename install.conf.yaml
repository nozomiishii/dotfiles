- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- shell:
    - command: ./setup/homebrew.zsh
      stdin: true
      stdout: true
      stderr: true
    - command: ./setup/node.zsh
      stdin: true
      stdout: true
      stderr: true

- link:
    ~/.zshrc:
    ~/.gitconfig:
    ~/.vimrc:

- create:
    - ~/Code
