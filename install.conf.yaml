- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:
# - create:
#     - ~/testing

- shell:
    - command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
