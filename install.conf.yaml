- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

# - create:
#     - ~/testing

- shell:
    - command: ./setup/homebrew.zsh
      stdin: true
      stdout: true
      stderr: true
