---
name: test
description: >-
  テストの書き方と進め方の正本。
  Claude Code で /test、Codex で $test と入力したとき、テストを書く・修正するとき、
  またはテスト作成を含む実装依頼を受けたときに使用する。
---

# /test

テストを書く・修正する時のスタイルと進め方。

## テスト先行

- 実装コードより先にテストを書く。テストが期待通りに失敗することを確認してから実装する。
- テストより先に書いた実装コードは消してやり直す。
- 3 回修正しても同じテストが通らない場合、設計を疑いユーザーに相談する。

## スタイル

- テストタイトルをつける場合は英語で記述する。
- テストの上に簡潔な日本語コメントを添える。
- テストファイルはソースと同じディレクトリに配置する。
- 1 テストで検証する振る舞いは 1 つにする。
- 各テストケースを自己完結させる。
  - 共通処理で Lifecycle Hooks は使わない。ヘルパー関数、`using`、テスト内に閉じた [Test hooks](https://vitest.dev/api/hooks.html#test-hooks) で組む。
- tautology test を避ける。実装をなぞる期待値ではなく、仕様から期待値を決める。
