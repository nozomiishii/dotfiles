---
name: pr
description: >-
  指定された PR の CI、コンフリクト、レビューコメントに対応し、
  マージできる状態まで持っていく。
disable-model-invocation: true
---

# PR

対象 PR の CI、コンフリクト、レビューコメントに対応し、マージできる状態まで持っていく。マージはしない。

## 対象 PR

- 引数に PR URL があれば、その PR を対象にする
- URL がなければ、現在のセッションが対象にしている PR を使う
- 対象を特定できなければ、ユーザーに PR URL を確認する

## 対応

CI の失敗やコンフリクトは原因を調べて直し、必要な検証を行って commit、push する。

未解決のレビューコメントは内容で分ける。

- 修正指示: 内容を確かめて修正し、検証、commit、push まで行う。thread には対応内容と commit を返信する
- 質問: ファイルを変更せず、現在のチャットで 1 件ずつ解説する。ユーザーの返答を待ってから次へ進む
- 判断できない内容: ファイルを変更せず、現在のチャットで 1 件ずつユーザーに確認する

変更後は PR の状態を確認し直す。新しい失敗やコメントがあれば続ける。質問やユーザーの判断待ちになったら、そこで止める。

レビュー thread の Resolve と PR のマージはユーザーが行う。
